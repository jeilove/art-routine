# Debug History - Art Routine

## 2026-03-27
- [x] Next.js Build 성패 여부 확인 (빌드 성공, Exit Code: 0)
- [x] `auth.ts`에 따른 Middleware Edge Runtime 호환성 문제 해결
- [x] ESLint 경고 및 에러 수정 (DailyNoteEditor `useEffect` 리팩토링 및 미사용 변수 제거)
- [x] Vercel 배포를 위한 `auth.config.ts` 분리
- [x] 실시간 동기화 훅 (`useSync`) 및 서버 액션 로직 점검
- [x] `next/font/google` 마이그레이션 (Next.js 권장 사항 반영)

### 오류 분석 및 조치
#### [에이전트실수] Auth.js 미들웨어 Edge Runtime 이슈 (수정 완료)
- **증상**: `NextAuth`에 `DrizzleAdapter`와 `db`가 포함되어 미들웨어(Edge Runtime)에서 Node.js 전용 API 호출 에러 발생.
- **조치**: `auth.config.ts`를 생성하여 DB와 무관한 공통 설정만 담고, 미들웨어에서는 이를 사용하도록 분리함. `auth.ts`는 서버 사이드에서 어댑터를 연결하여 사용함.

#### [에이전트실수] Auth Session 500 에러 (수정 완료)
- **증상**: 온라인 사이트 접속 시 `Art Routine v0.1.2`는 확인되나, `/api/auth/session` 호출 시 500 에러 발생.
- **분석**: `AUTH_SECRET` 환경 변수 누락 및 JWTID 매핑 로직 부재.
- **조치**: 
  - `auth.ts`에 `jwt` 콜백 추가 및 `trustHost: true` 설정.
  - 사용자에게 Vercel 환경 변수(`AUTH_SECRET`, `AUTH_GOOGLE_ID` 등) 등록 안내 후 적용 완료.

#### [에이전트실수] Build Error - Missing Imports (수정 완료)
- **증상**: 로그인 UI 추가 과정에서 `app/page.tsx`에 `useEffect`, `useRouter`, `motion` 임포트 누락으로 빌드 실패.
- **조치**: 누락된 임포트 구문 복구 및 로컬 빌드 테스트 후 재푸시 완료.

#### [에이전트실수] 데이터 복구(Restore) 시 동기점(Sync) 우선순위 설계 오류 (v0.2.3 수정 완료)
- **증상**: 온라인(Vercel) 환경에서 JSON 복구 직후 화면이 이전(서버 DB) 데이터로 되돌아가는 현상.
- **분석**: 로컬 복구는 성공했으나, 백그라운드에서 실행되는 `useSync`가 서버의 구형 데이터를 '최신'으로 오판하여 로컬을 덮어씌움.
- **조치**: 
  - `lib/actions.ts`에 `syncFullBackup` 서버 액션 추가.
  - `app/setup/page.tsx`에서 파일 복구 즉시 서버 DB를 강제 갱신(Push)하여 정합성 확보.
- **결과**: 수동 복구 데이터가 서버를 이기도록 설계 변경 완료.

### 현재 상태
- [x] 모든 빌드 에러 해결 (v0.2.3 로컬 및 Vercel 빌드 성공)
- [x] 비로그인(Guest) 모드 완전 개방 및 미들웨어 권한 체크 수정
- [x] 데이터 복원 기능의 서버 동기화 정합성 확보
- [x] 모바일 헤더 및 내비게이션 요소 겹침 오류 해결 (v0.2.12)

### 오류 분석 및 조치 (추가)
#### [디바이스대응] 모바일 헤더 요소 겹침 및 레이아웃 붕괴 (수정 완료)
- **증상**: PC에서는 정상이나 모바일 기기(실제 기기)에서 상단 헤더의 '팔레트/명화' 토글 버튼과 로그인 버튼이 겹쳐서 표시됨. 텍스트가 수직으로 깨지거나 다른 요소를 가림.
- **분석**: 
  - `flex` 컨테이너 내부 요소(제목, 토글, 로그인)에 `shrink-0`가 없어 좁은 화면에서 무분별하게 압축됨.
  - '🎨 팔레트'와 같이 긴 텍스트가 좁은 버튼 내에서 줄바꿈되며 레이아웃 파괴.
  - `flex-1`을 가진 제목 영역(`MY ART ROUTINE`)이 `min-w-0` 설정 부재로 공간 확보를 위해 다른 요소를 밀어냄.
- **조치**: 
  - 헤더 타이틀 텍스트를 모바일에서 축소 및 일부 숨김 처리.
  - 뷰 토글 버튼의 텍스트를 모바일 환경(`sm` 미만)에서 이모지 아이콘으로 대체.
  - 모든 상단 고정 요소에 `shrink-0` 및 `min-w-0`를 적용하여 최소 폭 보장 및 오버플로우 제어.
  - `AuthButton`의 'Creator' 라벨을 모바일에서 숨겨 가로폭 확보.
- **결과**: v0.2.12 배포를 통해 실제 모바일 기기에서의 레이아웃 안정성 확보.
- **태그**: #디자인보완 #에이전트실수(반응형 고려 미흡)

## 2026-03-28 (v0.3.1)
- [x] 시작일(startDate) 수동 조정 기능 추가 (/setup)
- [x] 서버 연동 시 클라이언트 시작일 보존 로직 적용 (lib/actions.ts)
- [x] '이전 루틴' 네비게이션 가인드 텍스트 추가

### 오류 분석 및 조치
#### [에이전트실수] 로그인 시 시작일 강제 초기화 (수정 완료)
- **현상**: 비로그인 사용자가 로그인 시, 서버에 시작일 정보가 없으면 '오늘'로 리셋되어 이전 기록을 볼 수 없게 됨.
- **분석**: `fetchUserData` 서버 액션에서 시작일 기본값을 `today`로 설정해둔 것이 클라이언트에서 유효한 시작일을 덮어씀.
- **조치**: 
  - 서버 액션에서 서버 데이터 부재 시 `null`을 반환하도록 수정.
  - 사용자가 직접 시작일을 수정하여 과거 데이터를 복원할 수 있도록 설정(/setup) 페이지에 날짜 선택기 도입.
- **결과**: 예기치 못한 초기화 시에도 사용자가 스스로 데이터를 복원할 수 있는 수단 확보.

## 2026-03-28 (v0.3.0)
- [x] 비로그인(Guest) 사용자의 자동 샘플 데이터 생성(MOCK) 복구 (app/page.tsx)
- [x] 루틴 사이클 전환(30일 경과) 안내 보강 - 대시보드 사이클 네비게이션 점검
- [x] 설정(/setup) 페이지 내 '샘플 데이터 로드' 버튼 전체 사용자 개방
- [x] 콘솔 버전 로깅 v0.3.0으로 상향 및 동기화 무안루프 가능성 차단

### 오류 분석 및 조치
#### [사용자혼란] 30일 주기에 따른 데이터 유실 오인 (수정 완료)
- **현상**: 비로그인 사용자가 어제는 데이터가 있었으나 오늘은 없다고 느끼는 현상.
- **분석**: 
  - 루틴 시작일로부터 정확히 31일째가 되어, 명화가 다음 작품(The Kiss)으로 넘어가며 새 캔버스가 나타난 것.
  - 기존 데이터는 '이전 주기(Prev Cycle)'에 보관되어 있으나, UI상 안내가 부족하여 초기화로 오해함.
- **조치**: 
  - `/setup` 페이지에 **🎨 샘플 데이터 로드** 버튼을 노출하여 사용자가 원할 때 언제든 다시 데이터를 채울 수 있도록 함.
  - `app/page.tsx`에서 데이터가 전혀 없는 Guest 진입 시 자동으로 샘플을 생성하도록 로직 복구.
- **결과**: 사용자가 빈 화면을 만났을 때 시스템 오작동으로 느끼지 않도록 '데이터 채우기' 제어권 제공.
- **태그**: #디자인보완 #사용자경험개선 #버전문제

### 현재 상태 (최신)
- [x] 30일 루틴 전환 시스템 설계 의도대로 동작 확인
- [x] 샘플 데이터 로더 UI 개선 완료
- [x] v0.3.0 배포 준비 완료 (v0.3.0 로컬 및 Vercel 빌드 성공 예상)
